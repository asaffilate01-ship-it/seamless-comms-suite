import { env } from "cloudflare:workers";import { requireInternalIdentity } from "@/lib/auth/authorize";
type Bindings={DB?:D1Database;ADMIN_EMAILS?:string};
const templates=[{id:"hospitality-core",name:"Hospitality Core",sector:"Hospitality",controls:12},{id:"food-safety-core",name:"Food Safety Core",sector:"Compliance",controls:18},{id:"waste-duty-care",name:"Waste Duty of Care",sector:"Waste",controls:14},{id:"construction-change",name:"Construction Change Control",sector:"Construction",controls:16},{id:"payment-risk",name:"Payment Recommendation Safety",sector:"Payments",controls:21}];
export async function GET(request:Request){const bindings=env as unknown as Bindings;const auth=requireInternalIdentity(request,bindings,["owner","admin","operator","viewer"]);if("response" in auth)return auth.response;if(!bindings.DB)return Response.json({mode:"simulation",packs:templates});const rows=await bindings.DB.prepare("SELECT id, name, sector, version, rules_json, status, created_at FROM policy_packs ORDER BY sector, name, version DESC LIMIT 100").all();return Response.json({packs:rows.results.length?rows.results:templates});}
