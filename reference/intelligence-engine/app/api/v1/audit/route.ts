import { env } from "cloudflare:workers";import { requireInternalIdentity } from "@/lib/auth/authorize";
type Bindings={DB?:D1Database;ADMIN_EMAILS?:string};
export async function GET(request:Request){const bindings=env as unknown as Bindings;const auth=requireInternalIdentity(request,bindings,["owner","admin","viewer"]);if("response" in auth)return auth.response;if(!bindings.DB)return Response.json({mode:"simulation",entries:[]});const url=new URL(request.url);const resource=url.searchParams.get("resource");const statement=resource?bindings.DB.prepare("SELECT id, user_id, action, resource_type, resource_id, metadata_json, created_at FROM audit_logs WHERE resource_type = ? ORDER BY created_at DESC LIMIT 200").bind(resource):bindings.DB.prepare("SELECT id, user_id, action, resource_type, resource_id, metadata_json, created_at FROM audit_logs ORDER BY created_at DESC LIMIT 200");const rows=await statement.all();return Response.json({entries:rows.results});}
