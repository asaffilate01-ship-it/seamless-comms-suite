# Merge instructions for the next developer / Lovable session

Integrate this additive Omniqora Enterprise Transformation package into the current `asaffilate01-ship-it/seamless-comms-suite` repository. Inspect the current branch and AGENTS.md first. Do not overwrite existing authentication, tenant handling, RRCI, adviser-learning or enterprise-AI work. Do not rewrite published Git history.

1. Add the files under `omniqora-update/` to the matching paths. Review `docs/INTEGRATION.md` and the capability status matrix.
2. Wire the new `/app/transformation` authenticated file route into the existing navigation and EN/DE dictionaries. Regenerate the TanStack route tree normally. Keep the current authentication middleware and query the explicit requested tenant membership on the server.
3. Deploy the Python service separately. Configure the trusted server URL and independent signing key. Do not put service/model/database secrets in the browser. Start with the evidence-only SQLite pilot; configure Neo4j and model providers only when the corresponding service is available and tested.
4. Verify a real owner can create a project, a verified tenant colleague can be granted a restricted project role, unrelated tenants/users cannot read it, viewers cannot write, a proposer cannot approve their own action or verify their own benefit, and revoked roles invalidate execution.
5. Run the supplied tests and the host build. Smoke-test all dashboard sections against the deployed service. Validate the responsive layout in an actual browser; this package's local DOM checks do not replace that.
6. Pilot a diagnostic with approved sources. Use Company planning to enter each company, its assets/workforce/services, current and target costs, objectives and transaction scope. Add the delivery mandate, diligence findings and TSA obligations. Generate a plan, resolve gaps and test independent review and stale-version behaviour. Initialise the six workstreams, assign actual owners, import source documents, review relationships, enter spending and reconciliation records, and record scope-specific acceptance criteria. Do not mark integrations live without testing their real endpoints.
7. Add one read-only connector first, chosen from the actual client's available systems. Next add dry-run proposals; enable a narrowly scoped external action only after its dedicated acceptance tests, approval policy and recovery process pass.

Version 0.3 already contains the provider/agent hub and three scoped read connector adapters. Merge `AIHubPanel.tsx`, the four `ai_*.py` modules, the expanded host RPC allowlist and service bootstrap together. Use `docs/AI-LAYER-SETUP.md` to configure one model and one source, bind their real tenant/project IDs and test with the customer. Existing contract fixtures are not live provider tests. Preserve independent action review and server-owned credentials; do not expose the signing key or operator registry through the browser.

Treat names of Oracle, SAP, Workday, Microsoft 365, cloud platforms, Fabric, Databricks, Power BI and compliance frameworks as configured targets. Their connectors, licences and certified outcomes are not magically supplied by the dashboard. Never replace evidence with invented scores or claim a production migration has happened because a planning action completed.
